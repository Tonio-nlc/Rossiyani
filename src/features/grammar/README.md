# features/grammar/

Display rules for Russian grammar visualization (POS colors, case ending underlines).

No detection logic — analysis data comes precomputed from the database.

## Allowed dependencies

- `@/types`
- `@/lib/constants`
