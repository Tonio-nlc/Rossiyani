import { ManualScholarShell } from "@/components/manual/manual-scholar-shell";

export default function ManualLayout({ children }: { children: React.ReactNode }) {
  return <ManualScholarShell>{children}</ManualScholarShell>;
}
