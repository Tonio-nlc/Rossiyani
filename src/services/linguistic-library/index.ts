import { indexFromAnalysis } from "./index-from-analysis";

/** Writes validated analyses into the LinguisticLibrary. */
export const linguisticLibraryIndexer = {
  indexFromAnalysis,
};

export { indexFromAnalysis };
